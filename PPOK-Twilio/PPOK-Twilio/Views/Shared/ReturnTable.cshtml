<table class="table table-striped table-responsive" id="myTable">
    <thead>
        <tr>
            <th>
                Name
            </th>
            <th>
                Description
            </th>
            <th>
                Sender Type
            </th>
            <th>
            </th>
        </tr>
    </thead>
    <tbody class="list">
        @if (Model != null)
            {
                foreach (var item in Model)
                {
                <tr>
                    <td id="name">
                        @item.Prescription.Patient.LastName, @item.Prescription.Patient.FirstName
                    </td>
                    <td id="descript">
                        @item.Prescription.Drug.Name
                    </td>
                    <td id="senderType">
                        @item.Prescription.Patient.Phone
                    </td>
                    <td>
                        <button class="btn btn-primary" data-toggle="modal" data-target="#myModalNorm">
                            Reject
                        </button>
                    </td>
                </tr>
            }
        }
    </tbody>
</table>